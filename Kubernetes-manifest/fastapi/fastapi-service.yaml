apiVersion: v1
kind: Service
metadata:
  name: fastapi-argo-service
  namespace: argocd-demo-ns
spec:
  type: NodePort
  selector:
    app: fastapi-test-argo
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
